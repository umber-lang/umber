; TODO: Might be a bit nicer to call umberboot.exe directly without cinaps and generate
; separate files

(cinaps
 (files prelude.ml)
 (libraries umberboot)
 (preprocess
  (pps ppx_jane)))

(library
 (name umber_std)
 (libraries core_kernel)
 (preprocess
  (pps ppx_jane)))
