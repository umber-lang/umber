; TODO: Consider doing this generation with a normal binary and including the files using
; a dune "site". We will probably want to do a similar thing for the runtime.

(cinaps
 (files prelude.ml)
 (libraries umberboot)
 (runtime_deps
  (file Std/Prelude.um))
 (preprocess
  (pps ppx_jane)))

(library
 (name umber_std)
 (libraries core_kernel)
 (preprocess
  (pps ppx_jane)))
