; TODO: Consider doing this generation with a normal binary and including the files using
; a dune "site", similarly to how the runtime is linked in.

(cinaps
 (files prelude.ml)
 (libraries umberboot)
 (runtime_deps
  (file Std/Prelude.um)
  (file prelude.ml))
 (preprocess
  (pps ppx_jane)))

(library
 (name umber_std)
 (libraries core_kernel)
 (preprocess
  (pps ppx_jane)))
