module : {
    val id : b -> b

    type Foo a b = Bar a | Baz b
    type alias Tuple a b = (a, b)

    val plus_one : a -> a
}

val id : a -> a
let id x = x

type Foo b a = Bar b | Baz a

type alias Tuple a a = (a, a)

let plus_one x = x + 1