module : {
  val id : b -> b

  type Foo a b = Bar a | Baz b
  type alias Tuple a b = (a, b)

  val plus_one : a -> a
  val plus_one' : Int -> Int
  val double : String -> String
}

val id : a -> a
let id x = x

type Foo b a = Bar b | Baz a

type alias Tuple a a = (a, a)

let plus_one x = x + 1

# FIXME: maybe move this somewhere else so we can actually see all the errors
module A : {
  type Int
  type alias Float = String

  val plus_one : Int -> Int
  val double : Float -> Float
} = {
  type Int = Float  # NOTE: This is actually `String` due to the type alias above

  let plus_one x = x +. 1.0
  let double s = s ++ s
}

let plus_one' = A.plus_one
let double = A.double