module : {
  type T = Int
  type Weather = | Rainy | Sunny

  val missing_function : T -> T

  module JustTypes : {
    type S
  }

  module MissingModule : {
    val missing : T
  }

  module A : {
    type A = | A1 | A2
    val a : A
  }
}

type T = String
type Weather = | Rainy

module A = {
  type A = | A1 | A2
  let a = "not a"
}
