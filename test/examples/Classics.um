# TODO: support matches like this:
  ```
  match n
  | 0
  | 1 -> 1
  | n -> n
  ```

let fact = match
  | 0 | 1 -> 1
  | n -> n * fact (n - 1) 

let fib n =
  match n
  | 0 | 1 -> 1
  | n -> fib (n - 1) + fib (n - 2)