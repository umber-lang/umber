# Can't go wrong with the classics

let fact = match
  | 0
  | 1 -> 1
  | n -> n * fact (n - 1) 

let fib n =
  match n
  | 0 | 1 -> 1
  | n -> fib (n - 1) + fib (n - 2)

let () =
  print "5! is:";
  print_int (fact 5);
  print ""